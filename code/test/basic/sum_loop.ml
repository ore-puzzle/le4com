let equal = fun a -> fun b ->
  if a < b then
    false
  else if b < a then
    false
  else
    true in


let sum = fun n ->
  loop c = (1, 0) in
    if n < c.1 then
      c.2 
    else
      recur (c.1 + 1, c.1 + c.2) in

equal (sum 10) 55;;
