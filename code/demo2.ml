let rec demo = fun x ->
  let a = x + 1 in
  let b = x + 2 in
  let c = x + 3 in
  let d = x + 4 in
  let e = x + 5 in 
  let f = x + 6 in
  let g = x + 7 in
  let h = x + 8 in
  let i = x + 9 in
  let j = x + 10 in
  let k = i + j in
  a + b + c + d + e + f + g + h + i + j + k in
demo 0;;
